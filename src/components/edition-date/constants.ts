/** Popover gap below the date trigger from the approved adaptive modal placement. */
export const DATE_PICKER_TRIGGER_OFFSET_PX = 8;

/** Viewport collision padding that keeps the calendar inside 320px-wide screens. */
export const DATE_PICKER_COLLISION_PADDING_PX = 16;
