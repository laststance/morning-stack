/** Stable namespace included in every edition collector advisory-lock key. */
export const EDITION_COLLECTION_LOCK_NAMESPACE =
  "morning-stack:edition-collection";

/** PostgreSQL extended hash seed used consistently when acquiring one collector lock. */
export const EDITION_COLLECTION_LOCK_HASH_SEED = 0;
