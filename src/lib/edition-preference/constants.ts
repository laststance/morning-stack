/** Browser cookie name shared by Home selection and Settings persistence. */
export const DEFAULT_EDITION_COOKIE_NAME = "morningstack-default-edition";

/** One-year browser persistence keeps the display choice across sign-out and routine cookie refreshes. */
export const DEFAULT_EDITION_COOKIE_MAX_AGE_SECONDS = 31_536_000;
